// prisma/schema/schema.prisma
generator client {
  provider = "prisma-client-js"
  output   = "../prisma/schema/generated/client"
}

datasource db {
  provider = "postgresql" // or your database
  url      = env("DATABASE_URL")
}
